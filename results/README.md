# Results Directory

This directory is used as the default output folder for inference scripts (`scripts/infer_batch.py` and `scripts/infer_image.py`).

Generated dehazed images will be saved here when running batch or single image testing.
